version: 2.1
jobs:
    build:
      steps:
        - checkout
        - run:
            name: test
            command: npm run test
        - run:
            name: release to chromatic
            command: npm run chromatic:project